{
  "name": "mediapipeposemax",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node main.js",
    "dev": "node main.ts",
    "all": "npm-run-all --parallel start dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "max-api": "^2.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/node": "^14.14.33",
    "tslint": "^6.1.3",
    "typescript": "^4.8.2"
  }
}
